@model IList<ProductOverviewModel>

@if (Model.Count > 0)
{
    <div class="section-padding section-bg product-grid home-page-product-grid">
        <div class="section-width flash-sale-section">
            <div class="flash-sale-container">
                <div class="section-icon-title">
                    <img src="@Url.Content("~/Themes/MarketTheme/Content/images/flash-icon.png")" alt="@T("Flash.Icon")" class="flash-icon" />
                    <h2 class="section-title">@T("HomePageProducts.Title")</h2>
                </div>
                <a href="@Url.RouteUrl("AllProducts")" class="view-all-button desktop-only">
                    @T("HomePageProducts.Button.ViewAll")
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"
                        stroke-linejoin="round">
                        <line x1="5" y1="12" x2="19" y2="12"></line>
                        <polyline points="12 5 19 12 12 19"></polyline>
                    </svg>
                </a>
            </div>

            @if (Model.Any())
            {
                <div id="regular-discount-products" class="item-grid regular-discount-row">
                    @foreach (var item in Model)
                    {
                        <div class="item-box">
                            @await Html.PartialAsync("_ProductBox", item)
                        </div>
                    }
                </div>
            }

            <div class="view-all-mobile mobile-only">
                <a href="@T("HomePageProducts.Button.ViewAll.Url")" class="view-all-button">
                    @T("HomePageProducts.Button.ViewAll")
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"
                        stroke-linejoin="round">
                        <line x1="5" y1="12" x2="19" y2="12"></line>
                        <polyline points="12 5 19 12 12 19"></polyline>
                    </svg>
                </a>
            </div>
        </div>
    </div>
}

<script asp-location="Footer">
    function startCountdown() {
        const hoursElement = document.getElementById('hours');
        const minutesElement = document.getElementById('minutes');
        const secondsElement = document.getElementById('seconds');

        const targetHourCOL = 18;
        const targetMinuteCOL = 0;
        const targetSecondCOL = 0;

        function updateTimer() {
            const now = new Date();

            let targetDate = new Date(Date.UTC(
                now.getUTCFullYear(),
                now.getUTCMonth(),
                now.getUTCDate(),
                targetHourCOL + 5,
                targetMinuteCOL,
                targetSecondCOL
            ));

            if (now.getTime() > targetDate.getTime()) {
                targetDate.setUTCDate(targetDate.getUTCDate() + 1);
            }

            const diff = targetDate.getTime() - now.getTime();

            if (diff <= 0) {
                hoursElement.textContent = '00';
                minutesElement.textContent = '00';
                secondsElement.textContent = '00';
                return;
            }

            const hours = Math.floor(diff / (1000 * 60 * 60));
            const minutes = Math.floor((diff % (1000 * 60 * 60)) / (1000 * 60));
            const seconds = Math.floor((diff % (1000 * 60)) / 1000);

            hoursElement.textContent = String(hours).padStart(2, '0');
            minutesElement.textContent = String(minutes).padStart(2, '0');
            secondsElement.textContent = String(seconds).padStart(2, '0');
        }

        updateTimer();
        setInterval(updateTimer, 1000);
    }

    if (document.getElementById('hours') && document.getElementById('minutes') && document.getElementById('seconds')) {
        document.addEventListener('DOMContentLoaded', startCountdown);
    }
</script>